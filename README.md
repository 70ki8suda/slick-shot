# SlickShot

SlickShot is a macOS utility for transient screenshots.

It captures a selected screen region with a native-feeling shortcut, keeps the result in a temporary thumbnail stack instead of saving it to the Desktop, and removes screenshots automatically after successful drag-and-drop or a short retention period.
